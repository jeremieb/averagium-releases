# averagium-releases
Release artifacts for Averagium — hosts the appcast, changelog, and per-release notes served via GitHub Pages for Sparkle auto-updates. Source code lives in the private averagium-projects repo; this repo contains no source, only generated update metadata and GitHub Release binaries.
